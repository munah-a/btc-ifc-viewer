# BTC IFC Viewer — Audit Findings Registry

> Source: full multi-agent implementation audit, 2026-07-05 (55 agents, all factual claims adversarially
> verified against the code; one claim refuted by live-running the app). This file is the **single source
> of truth** for defect specs. Implementation tasks in `IMPLEMENTATION_PLAN.md` reference these IDs.
> **Do not re-audit these areas — trust the file:line references and verify only after fixing.**
>
> Line numbers refer to the repo state at commit `498c9c1`. If the file has since changed, locate the
> pattern described rather than the exact line.

Verdicts: ✅ confirmed by adversarial verifier · 🔬 confirmed by live-running the app · ⚠️ unverified
(session limit) but corroborated by ≥2 independent auditors · ❌ refuted.

## A. Architecture (viewer.ts unless noted)

| ID | Sev | Verdict | Location | Issue → Fix |
|----|-----|---------|----------|-------------|
| A1 | High | ✅ | `viewer.ts:1443-1445, 1480-1487, 1500-1507` | XSS: IFC-derived strings (storey/class names, modelId) interpolated into innerHTML unescaped in renderSpatialTree / renderClassFilters / renderLevelFilters. Sibling renderers use `escapeHtml` (line 312) correctly. → Apply `escapeHtml` in these paths; add CSP meta tag. |
| A2 | High | ✅ | `viewer.ts:1153-1159, 1163-1167` | Runtime hard-dependency on unpkg (web-ifc WASM) + thatopen.github.io (fragments worker, unpinned). Local `public/web-ifc.wasm` unused (referenced only by dead code at 2935). → Self-host: WASM from `node_modules/web-ifc/web-ifc.wasm`, worker from `node_modules/@thatopen/fragments/dist/Worker/worker.mjs` (v3.3.5), copied to `public/` at build. |
| A3 | Med | ✅ | `viewer.ts:2932-3296` | 365 lines dead code (`_unused_extractAndApplyMaterialColors`) keeps whole web-ifc JS API in bundle. Also dead: `setRightView/LeftView/BackView/BottomView` (3485-3499), `listen()` (357-363). → Delete all; git history preserves. |
| A4 | High | ✅ | `viewer.ts:365-459, 5275` | Untestable by construction: DOM cache of ~90 required IDs throws at construction; bootstrap at module scope outside try/catch → blank page on any renamed ID; pure logic unexported. → Extract pure modules (see Wave 2); lazy/injected DOM; guarded bootstrap. |
| A5 | Med | ✅ | `viewer.ts:575-616, 545-569` | `destroy()` never called; window/document listeners (915, 918, 1021) escape the abort-signal patch. `console.warn` permanently monkey-patched (641-655); patch clobbers caller `signal` option. → Wire destroy to HMR dispose or delete the machinery; route global listeners through `listen()`. |
| A6 | Med | ✅ | `viewer.ts:1263, 2854, 3298-3378` | Model identity: FIFO `pendingModelMetaQueue` can mis-attribute file names; polling loops (40ms/8s/30s, isBusy 10s); ~130 lines alias-resolution (2326-2414) papering over it. → Key metadata by the name passed to `ifcLoader.load` (becomes `model.modelId`); collapse alias layer. |
| A7 | Med | ✅ | `viewer.ts:4955-4999 vs 5041-5079` | `importViewerState` duplicates `restoreLocalState`; unguarded `parsed.viewpoints` crashes on import without viewpoints array (4969); dead legacy fallback `'xray'/'shaded'` (4963). → Single `applyPersistedState()` with schema validation/defaults. |
| A8 | Med | ✅ | `viewer.ts:1245, 2351, 3649, 3760, 3822` | ThatOpen boundary fully untyped (~20 `any` sites) incl. private `_controls` access (3649) on ^-ranged deps. → Define `FragmentsModelLike` interface for the ~10 methods used; isolate `_controls` behind one function. |
| A9 | Med | ✅ | `viewer.ts:2420-2433` | Errors classified by message substring `'model not found'`; suppressed branch logs nothing. → Named predicate + console.debug; typed errors if library exposes them. |
| A10 | Med | — | `viewer.ts:2876-2882, 2904-2907` | Load timeout doesn't cancel: late model registers anyway (ghost), steals next file's metadata from FIFO. → `.catch` before race; record stale id; ignore/dispose late arrivals. (Same root as A6.) |
| A11 | Med | — | `viewer.ts:1292-1298, 2482+` | Full innerHTML re-render collapses `<details>` state, rebuilds slider mid-drag; mixed wiring (delegation vs per-item listeners). → Standardize on delegation; preserve expanded state. |
| A12 | Low | — | `viewer.ts:3468/3501, 770-796, 3979/4044, 1461` | Duplicated blocks: fitToModel≡setHomeView; 3 section handlers; array-preview ×2; spatial-tree isolate reimplemented. → Collapse each. |
| A13 | Low | — | `viewer.ts:492, 937, 3822 + styles.css` | Brand magenta `#c8145c` in 4 places; magic numbers (chunkSize 360, timeout 120000, xray 0.28…). → Promote to constants block; brand color from CSS custom property. |
| A14 | Low | — | `tsconfig.json:12-17` | Inert emit options under noEmit; missing noUnusedLocals/noUnusedParameters/noUncheckedIndexedAccess. → Clean + enable. |
| A15 | Low | — | `viewer.ts:5259-5272` | FPS counter measures rAF cadence not render work; overwrites load metrics within 1s. → Hook renderer events; separate status slot. |
| A16 | Low | ✅ (found by tsc in W0) | `viewer.ts:2242-2278` (pre-W0.4 lines) | `applyHiddenLineColors` / `applyConsistentLighting` never called anywhere (same class of dead code as A3, not listed there), leaving `resetModelColors` / `restoreOriginalLighting` as permanent no-ops guarded by never-set flags. → Dead pair deleted in W0.4 (deletion forced by the new `noUnusedLocals` gate); the no-op guard functions remain called from `setVisualStyle` — collapse them when visual styles are extracted (W2.3). |

## F. Features (broken behavior)

| ID | Sev | Verdict | Location | Issue → Fix |
|----|-----|---------|----------|-------------|
| F1 | High | ⚠️ (API shape confirmed in node_modules) | `viewer.ts:2761-2776` | Search crashes on any hit: fragments v3.3 `getItemsData` returns `{value,type}` ItemAttribute objects; `escapeHtml(obj)` throws TypeError; results never render. Every other path unwraps via `readPrimitiveValue`. → Unwrap attributes in `searchElements`. |
| F2 | High | 🔬 | `viewer.ts:4561, 4934` | Screenshots + viewpoint snapshots are **blank transparent PNGs**: `toDataURL()` on non-preserved WebGL buffer with no render-before-capture (verified by running the app: 16KB transparent PNG; control render-then-capture produced real 532KB image). → Render immediately before capture (or preserveDrawingBuffer); then downscale viewpoint thumbnails (~320px JPEG) and show them in the list. Supersedes the refuted localStorage-quota claim (❌ A-quota): quota risk only becomes real once capture is fixed — cap/downscale then. |
| F3 | High | ⚠️ | `viewer.ts:2301-2307, 1300` | Loading any model resets X-ray/edges: `setVisualStyle` clears toggles; `onModelAdded` calls it per registration; persisted `xray:true` can never survive. → Move toggle-reset to explicit user style change only. |
| F4 | Med | ⚠️ | `index.html:644-647` vs `viewer.ts:2104` | 500ms grid-reset hack races init; default grid state nondeterministic; can clobber persisted preference. → Delete hack; one default in viewer.ts. |
| F5 | Med | ⚠️ | `viewer.ts:3908+, 3838+` | Property units fabricated from label keywords, not IfcUnitAssignment. → Read the model's unit assignment; keep keyword inference as fallback only. |
| F6 | Med | ⚠️ | — | No model unload; fragments/indices/worker memory accumulates until reload. → Add per-model dispose in federation panel (fragments.core disposal API). |
| F7 | Low | — | `index.html:101, 194` vs `viewer.ts:748-759` | "Show All" menu runs show-selection, errors with no selection; tooltip disagrees. → Rename or repoint to Reset Visibility. |
| F8 | Low | — | `viewer.ts:849` | Theme toggle force-overwrites custom background. → Per-theme background memory; only swap defaults. |
| F9 | Low | — | `viewer.ts:4693+` | Issues capture only first model's elements in multi-model selection; orphan pins render with no model loaded. → Iterate all models; hide pins with no resolvable model. |
| F10 | Low | — | `viewer.ts:4633-4638` | Viewpoint apply restores clipping without gizmo-visibility fix; section buttons out of sync. → Reuse section-plane creation path. |
| F11 | Low | — | filters | Disjoint class∩level filter silently hides entire model. → Warn/empty-state when intersection is empty. |

## U. UI/UX

| ID | Sev | Verdict | Location | Issue → Fix |
|----|-----|---------|----------|-------------|
| U1 | **Critical** | ✅ | `styles.css:1960-2110`, `index.html:519-538` | **All panels unreachable ≤1023px**: `.drawer-open` never added by any JS; 5 `[data-mobile-nav]` buttons have no handlers; phone hides dock+status bar too (2075, 2112) → canvas + upload FAB only, no error channel. → Wire drawers/backdrop/bottom-nav; move `#viewerDock` out of `.app-toolbar`; route errors through toasts. |
| U2 | High | ✅ | `styles.css:1952, 2074`, `index.html:34-77` | Theme/style/grid/background live only in menubar, hidden ≤1023px — no light theme on tablets (sunlight!). → Relocate to responsive settings surface; default from `prefers-color-scheme`. |
| U3 | High | ✅ | `styles.css:1643, 1683, 2033, 1516, 1373, 1463` | Hardcoded dark rgba breaks light theme (flyouts ~1.5:1 contrast, white-on-white hover, invisible separators). → Replace with theme tokens + light overrides. |
| U4 | High | ✅ | `viewer.ts:2916, 4997, 5037, 2430` | Errors only as 11px status text (hidden on phones); `showToast` used only for successful deletes. → All catch paths → error toast; loading overlay gets error state + Retry. |
| U5 | Med | ✅ | `styles.css:348, 430, 1669, 1724` | Icon-only buttons announce as `upload_file`/`3d_rotation` (ligature text; labels display:none'd; zero aria-hidden in repo). Google Fonts failure shows raw words. → aria-hidden on icons + aria-label per button; prefer inline SVG (rebrand covers this). |
| U6 | Med | ✅ | `viewer.ts:2771+, 1451+, 4681+, 4806+` | Search/tree/viewpoint/issue rows are clickable divs — no keyboard/SR path (model browser does it right with buttons/details). → `<button>` rows + list semantics. |
| U7 | Med | ✅ | `index.html:348, 14, 594-624` | Broken ARIA: tablist without tabs; menus without aria-expanded/Escape; `role=application` on whole shell. → Complete tabs pattern; menu semantics; drop role=application. |
| U8 | Med | ✅ | `viewer.ts:5216-5257, 5117` | Confirm dialog: Escape leaks to global shortcuts (cancels tools behind dialog), no focus trap, focus not restored, destructive button pre-focused. → `<dialog>.showModal()`; focus Cancel; restore invoker. |
| U9 | Med | — | `index.html:552-587` | Splitters mouse-only (no touch/keyboard), 4px hit target. → Pointer Events + setPointerCapture; ~12px hit area; role=separator + arrows. |
| U10 | Med | — | `styles.css:436-484, 657-715, 752, 2114` | ~120 lines dead CSS; dock styled 3× (specificity war). → Delete; restructure dock mobile-first. |
| U11 | Low | — | `styles.css:1041, 1813, various` | 10px accent titles ~3.2:1 contrast; toasts overlap view cube; 'E' double-bound; no keyboard camera; no busy feedback on search/props; pervasive 9-11px type. → Batch with rebrand. |

## P. Performance & delivery (measured)

| ID | Sev | Verdict | Measurement | Issue → Fix |
|----|-----|---------|-------------|-------------|
| P1 | High | ✅ | `index-*.js` = 5,725.61 kB (999.32 kB gzip), 1 chunk | No code-splitting/dynamic imports; everything blocks first paint. → `manualChunks` (three / thatopen / web-ifc) + dynamic-import the IFC load path on first file-open. |
| P2 | High | ✅ | see A2 | Init awaits two external CDNs before usable. → Self-host (A2). |
| P3 | High | ✅ | `viewer.ts:3724-3749, 1220-1227, 1072-1083` | EdgesGeometry rebuilt for every mesh per pointer-move during gizmo drag & per opacity-slider tick — O(triangles)/frame. → Cache per geometry UUID; update matrices only; debounce slider. |
| P4 | Med | ✅ | `viewer.ts:2861-2874` | IFC parse on main thread (worker only does culling) — UI freezes on big models. → Move web-ifc into a worker (Wave 5); mitigate first with progress + chunked yields. |
| P5 | Med | ✅ | dist = 28.20 MB | ~78% dead weight: 21.4MB sample IFCs never fetched by deployed app (e2e reads from disk), 1.26MB unused WASM (used after A2), favicon 0 bytes. → Move samples to `e2e/fixtures/`; real favicon. |
| P6 | Med | ✅ | continuous rAF + heaviest preset default | No on-demand rendering; outlines+gloss+SMAA default; battery drain. → On-demand render mode (esp. embeds); lighter default style. |
| P7 | Med | ✅ | `viewer.ts:1348` | Indexing: hundreds of sequential worker round-trips; browser HTML fully rebuilt multiple times per load. → Parallelize chunks; render once at end. |
| P8 | Med | ✅ | `vite.config.ts:18` | Hardcoded base `/btc-ifc-viewer/`; GH Pages fixed 10-min cache; no vercel.json despite Vercel link — the two targets can't both work. → Env-driven base + vercel.json headers. |
| P9 | Low | — | `viewer.ts:3563+, styles.css` | 26 hotspot style writes per camera frame; 6 backdrop-filter surfaces over live WebGL. → Batch transforms; reduce blur layers (rebrand). |

## T. Testing & CI

| ID | Sev | Verdict | Location | Issue → Fix |
|----|-----|---------|----------|-------------|
| T1 | **Critical** | ✅ | `.github/workflows/deploy.yml:28-29` | Every push to main → build → production. No tests/typecheck/lint/audit. Playwright CI config is dead code. → CI gate (typecheck, lint, e2e) before deploy; PR workflow. |
| T2 | High | ✅ | `tsconfig.json`, `viewer.ts:1139` | `tsc --noEmit` FAILS today (`import.meta.env` needs `"types":["vite/client"]`); nothing anywhere runs tsc. → Fix types entry; add typecheck script; run in CI. |
| T3 | High | ✅ | `package.json` | 7 vulns: critical fast-xml-parser (CVSS 9.3) in RUNTIME @thatopen chain (fix non-major), high rollup + vite (vite fix = major v8), 4 moderate. No Dependabot. → `npm audit fix` now; plan vite major; add Dependabot + CI audit gate. |
| T4 | High | ✅ | `playwright.config.ts:35`, `viewer.ts:1139-1144` | E2E can only test dev server: hooks need `window.__viewer` which is DEV-gated; production artifact untestable; base-path/minification failures invisible. → Gate handles behind explicit `VITE_E2E` define; test `vite build && vite preview`. |
| T5 | Med | ✅ | `e2e/viewer.spec.ts:197-557` | One 557-line test; seven 750ms sleeps; ~20 exact status-copy assertions; effective 36-min timeout; screenshots never compared. → Split into describe blocks w/ fixtures; wait on state not sleeps; assert aria/data attrs. |
| T6 | Med | ✅ | `e2e/viewer.spec.ts:184-189+` | Selection tested via private internals; canvas clicks, keyboard shortcuts, drag-drop, federation multi-model: 0% coverage. → Frozen `window.__viewerTestApi` contract; add real-interaction tests. |
| T7 | Med | ✅ | — | Zero unit tests / no runner. → Vitest (pairs with Wave 2 extraction). |
| T8 | Low | — | `tsconfig.json:19`, `playwright.config.ts:26` | e2e + configs type-checked by nothing; chromium-only. → tsconfig.node.json; add firefox/webkit happy path later. |
| T9 | Low | — | `deploy.yml` | Actions pinned to mutable major tags. → Pin to SHAs. |
| T10 | High | 🔬 (found by the W0 suite run against the prod build) | `e2e/viewer.spec.ts` (monolith `openDock` helper, pre-W0.9) + `styles.css` dock rules | The suite could not pass on current main at all: `openDock` clicked `[data-dock-toggle][title=…]`, but since the ui-overhaul merge those toggles are `display:none` at desktop widths (dock tools render inline), so the click never became actionable — and with Playwright's default unlimited `actionTimeout` each such step hung for the full 12-min test timeout (the T5 "36-min effective timeout" in practice). → Fixed in W0.9: dock-tool buttons clicked directly; `actionTimeout: 15s` + `navigationTimeout: 30s` set in playwright.config.ts. The dock-toggle UI itself is redesigned in W3.2/W3.3. |

## Verified-good (don't "fix")

- `escapeHtml` correctly used in renderModelBrowser / renderFederatedTree / property sections / search result *template* (the F1 crash is pre-escape).
- Strict TypeScript ON; typed state schemas; discriminated unions for modes.
- AbortController listener strategy (for `dom`-cached elements); request-id guard on load path; `fireAndForget` routing.
- ThatOpen v3.3 API usage in Hider/Clipper/Marker/measurements/highlight verified correct against installed lib.
- deploy.yml secrets/OIDC handling is correct; concurrency group sane.
- GLB-relevant: all meshes reachable via three scene graph (export feasible).

## Post-audit findings (added during execution)

| ID | Sev | Verdict | Location | Issue → Fix |
|----|-----|---------|----------|-------------|
| T11 | High | ✅ (CI run 28756787701, PR #7) | `e2e suite on ubuntu CI` | E2E suite passes locally (GPU) but fails wholesale on the 2-core SwiftShader runner: the continuous render loop at the heaviest default postprocessing preset (P6) starves rAF, so Playwright's element-stability check times out (~uniform 15s actionTimeout failures on nearly every click; run got through install/build fine post-lockfix). → CI-stabilization: under VITE_E2E switch to cheapest visual style after fixture load; shrink test viewport (1600×1000 → 1280×720); raise CI actionTimeout (~45s) and consider `--enable-unsafe-swiftshader` launch arg; long-term fix is P6 on-demand rendering (W5.3). |
| T12 | High | ✅ (CI run 28767311085, PR #14) | `e2e/console-clean.spec.ts` test-timeout + `viewer.spec.ts` U4 error-state wait | Local `ci:local` passed 16/16 under CI=1 but GitHub failed 2 timeout-bound tests — a fast dev GPU box does not reproduce the 2-core runner's wall clock even with SwiftShader flags. (a) console-clean's own `test.setTimeout(10min)` was not CI-scaled and the full sweep needs ~15min on the runner (passed at 12.2m once raised); (b) U4 — see T13 (turned out to be a logic bug, not just timing). → console-clean `test.setTimeout` CI-scaled to 30min + waitForStatus 45s. **Root cause = local↔CI parity: added opt-in `e2e/_cpu-throttle.ts` (`E2E_CPU_THROTTLE`) — but measured that CDP throttling does NOT slow SwiftShader GL / the fragments worker, so render-bound CI budgets must instead carry ~3x margin.** |
| T13 | High | ✅ (CI run 28771992021, PR #14 re-run) | `viewer.ts` loadIfcFile + `e2e/viewer.spec.ts` U4 | U4 (corrupt-file error surfacing) failed on GitHub even with a 150s wait (> the app's 120s load timeout): the test fed `NOT-AN-IFC-FILE` and relied on web-ifc's **undefined behavior for garbage** — locally it threw (error shown ~31s), but on the CI runner web-ifc evidently parsed it as an empty model (silent success), so `is-error` never appeared. → New pure `src/core/ifc-format.ts` `isProbablyIfc()` sniffs the `ISO-10303-21` STEP header BEFORE web-ifc; non-IFC input throws instantly → showLoadError → is-error, deterministic on every platform. Also a real UX win (instant "not a valid IFC" vs 120s timeout / silent empty load). Unit-tested. |
| A17 | High | 🔬 (PO live-verified in prod build 2026-07-06) | `viewer.ts getModelBoundingBox` (~2884) | **Fit and Section fail immediately after load** ("No model to section"; Fit doesn't frame) — `getModelBoundingBox` used `three` `expandByObject(model.object)`, which reads an EMPTY box until the fragments worker streams meshes. The model is loaded/indexed (tree, counts, `objectCount:1`) but geometry bounds aren't ready. e2e masked it: its worker-shared fixture runs many camera/render ops before the section test, so geometry has streamed by then. Pre-existing (predates W3); exposed by PO clean-load live verification. Also hurts slow/weak GPUs (C6 field tablets). → Use the fragments model's data-driven `.box` (available at load), transformed to world space by the object matrix (federation offsets), with `expandByObject` fallback. Added `box` to `FragmentsModelLike` (A8 boundary) + a fresh-load e2e guard (section works before any camera op). |
| A18 | High | 🔬 (PO screenshot verify, 2026-07-06) | `src/ui/icons.ts setIcon` (W3) | **All hydrated icons rendered 0×0 with black fill** — invisible across the tool rail, view-control overlay, right tab strip and topbar actions (the rebrand looked broken vs the design). `setIcon` used `DOMParser.parseFromString(str,'image/svg+xml')` + node-adoption via `replaceChildren`; the adopted `<svg>` had its `width`/`height`/`fill="currentColor"` presentation attributes ignored (computed width 0px, fill rgb(0,0,0)) — effectively non-functional. Console was clean (silent). e2e/axe passed (buttons still clickable/labelled) so automated gates missed it; caught by the PO rebrand **screenshot** gate (acceptance criterion 4). → `setIcon` uses `el.innerHTML = icon(...)` (HTML parser namespaces the svg correctly; icon strings are static constants, not user data — not an A1 surface). Verified: rail svg now SVG-namespaced, 20×20, fill currentColor; empty-state 52×52. Regression guard in `e2e/icons.spec.ts`. |

## S. Security (W4 hosting-API adversarial review, 2026-07-06 — all fixed on wave/4-embed, each with a regression test)

| ID | Sev | Verdict | Location | Issue -> Fix |
|----|-----|---------|----------|-------------|
| S1 | **Blocker** | OK (fixed + concurrency test) | `api/_lib/hosting.ts handleUpload` + `storage.ts` | **Quota-bypass TOCTOU race**: active-upload quota was non-atomic count-then-act - N concurrent POSTs all passed `countActiveByOwner` before any track call, storing > maxActive. -> Added atomic `reserveOwnerSlot(ownerId,id,maxActive,ttl)`: reserve BEFORE storing; roll back on downstream failure. InMemory reserves synchronously (single turn, no await between add and bounded check); Redis uses sadd+scard+srem-on-overflow (fail-closed). Test fires maxActive+5 concurrent uploads via Promise.all and asserts exactly maxActive stored, rest 409. |
| S2 | **Blocker** | OK (fixed + spoof test) | `api/_lib/http.ts clientIp` | **X-Forwarded-For owner/rate spoof**: trusted the LEFT-most client-supplied XFF token -> a random XFF per request minted a fresh quota + rate bucket (total bypass). -> `clientIp` prefers platform-trusted `x-real-ip` / `x-vercel-forwarded-for`, and for `x-forwarded-for` takes the RIGHT-most (appended-closest) hop, never the client left. Tests: spoofed left-most XFF (constant real right hop) does NOT mint a new bucket; x-real-ip wins over a client XFF. |
| S3 | Major | OK (fixed + adapter test) | `api/_lib/storage.ts createRealStorage.deleteFrag` | **deleteFrag pathname vs URL**: `@vercel/blob` del deletes BY URL but we passed `meta.blobPath` (pathname) -> delete no-op -> orphaned blobs, TTL never reaped. -> `deleteFrag(fragUrl)` interface; delete + cron pass `meta.fragUrl`; real adapter calls `blob.del(fragUrl)`. Test asserts the injected blob.del is called with the full URL; InMemory tolerates URL or bare path. |
| S4 | Major | OK (fixed + cache test) | `api/_lib/hosting.ts` (blob cache) | **TTL not enforced on the direct-CDN read path**: `.frag` had a 1-year immutable cache, so expired/deleted content stayed CDN-fetchable. -> cache max-age at Blob put-time is now the entitlement TTL (7 days), NOT 1 year, so expired/deleted content also falls out of CDN cache. Paired with S3's working deletion. Test asserts put-time max-age == ttlDays*86400. |
| S5 | Minor | OK (fixed + fail-closed test) | `api/_lib/hosting.ts handleCronCleanup` | **Cron failed open when CRON_SECRET unset**: an unset secret let the sweep run unauthenticated. -> FAIL CLOSED in production (`process.env.VERCEL`): missing secret -> 500, no sweep. Off-platform (local tests) still allowed. Tests: production+no-secret -> 500 misconfigured; local+no-secret -> 200. |
| S6 | Minor | OK (fixed + test) | `api/_lib/http.ts verifyToken` | **Hand-rolled XOR compare** for the delete token. -> uses Node `crypto.timingSafeEqual` on the fixed-length hex digests (a true constant-time primitive); length-guarded. Tests: correct token accepted, wrong/empty/mismatched-length rejected without throwing, deterministic over many trials. |
| S7 | Minor | OK (fixed + test) | `api/_lib/http.ts ownerSalt` | **BTC_OWNER_SALT dev fallback** was used even in production (predictable salt -> guessable owner buckets). -> fail-closed: throws in production (`VERCEL`) if unset; dev/test keeps a fixed fallback. Tests: prod+no-salt throws; prod+salt resolves; off-platform fallback resolves. |
| S8 | Minor | OK (fixed + unit + e2e) | `src/core/url-state.ts isAllowedModelUrl` + `embed.ts` / `viewer.ts` | **Embed rendered an arbitrary `?m=` URL** (BTC-branded phishing / content-injection vector). -> `isAllowedModelUrl` allowlist: same-origin + configured origins (`VITE_ALLOWED_MODEL_HOSTS`) + the Vercel Blob CDN (`*.public.blob.vercel-storage.com`); http(s) only; look-alike-host safe. Embed AND full-app `?m=` boot-load reject disallowed hosts up front with a localized message (`embed.errorBlockedUrl`, en+de) - no poster, no engine, no fetch. Unit tests (allow/reject/scheme/look-alike) + e2e (foreign host blocked, never fetched). |

## W5-fixups (adversarial review of Wave 5, verifier-endorsed — all fixed on `wave/5-perf`, each with a regression test where feasible)

> Six groups (R render-parity, W worker-resilience, C persistence/data-integrity, E edge-leak, P PWA-offline, M perf/quality). `ci:local` green. Two items explicitly DEFERRED to W6 (see below).

| ID | Sev | Verdict | Location | Issue -> Fix |
|----|-----|---------|----------|-------------|
| R1 | High | OK (fixed + e2e) | `viewer.ts` initEngine + `startSectionDragPump`/`stopSectionDragPump` | **Section-plane gizmo drag froze the clipped view** — dragging a clip-plane arrow re-clips geometry but the camera is disabled during the drag, so the camera 'update' render pump never fires (MANUAL mode). -> Wire `clipper.onBeforeDrag`/`onAfterDrag` (the component-level hooks that wrap each SimplePlane's onDraggingStarted/onDraggingEnded in @thatopen ~3.3) to start/stop a bounded per-frame rAF render pump; cancelled on drag end + destroy. |
| R2 | High | OK (fixed) | `viewer.ts` initEngine | **Measurement live rubber-band froze between clicks** — pointer-move mutates the preview geometry but `pickAndSelect` only requestRendered AFTER a `.create()` click. -> Subscribe length+area `onPointerMove`/`onPointerStop` (inherited from the abstract Measurement base) to `requestRender()`. |
| R3 | Med | OK (fixed + e2e) | `viewer.ts clearMeasurements` + `finishAreaMeasurement` keyboard action | **clearMeasurements() and Enter->finish-area left a stale frame** — both mutate the scene with no camera motion. -> Add `requestRender()` at the end of `clearMeasurements()` and after `endCreation()` in the finish-area action. (clearSections already requestRendered.) e2e asserts renderRequestCount advances on both clear actions (new `renderRequestCount()` test hook). |
| W1 | High | OK (fixed + unit) | `core/ifc-conversion-client.ts onWorkerError` | **Dead worker reused forever** — onWorkerError rejected/cleared pending jobs but left the dead worker assigned, so ensureWorker() returned it and every later convert() hung. -> terminate + null the worker after rejecting jobs so the next convert() spawns fresh. |
| W2 | High | OK (fixed + unit) | `core/ifc-conversion-client.ts cancel` + `viewer.ts` timeout path (~2297) | **Abandoned 120s-timeout conversion never cancelled** — kept the shared worker busy (stalled retry, doubled web-ifc memory) and its stale progress callbacks kept mutating the error-state overlay. -> Add `cancel()` (terminate+null worker, reject/remove abandoned job); viewer calls it on timeout. `disposeStaleModel`/`markStale` path stays sound (cancelled loadPromise rejects -> consumeStale). `terminate()` delegates to `cancel()`. |
| C1 | High | OK (fixed + unit fake-IDB) | `core/frag-cache.ts withStore` | **IndexedDB readwrite resolved before commit -> silent write loss** — resolved on `request.onsuccess`, so a commit-time abort (quota on flush / tab unload) was swallowed; a "saved" frag never persisted, restore silently dropped the model. -> readwrite ops resolve on `tx.oncomplete`, reject on `tx.onabort`/`tx.onerror` (capturing request.result); readonly reads still resolve on onsuccess. Fake-IDB unit test guards the abort-rejects path (also covers M3). |
| C2 | Med | OK (fixed + unit adapter) | `core/frag-cache.ts` (delete/keys were dead) + `viewer.ts unloadModel`/`saveSession`/`evictUnreferencedFrag` | **Frag cache grew unbounded** — delete/keys/clear were never called. -> unloadModel evicts the removed model's frag when unreferenced by any live model AND the persisted session, then prunes orphaned IDB entries; explicit saveSession prunes too. Never deletes a frag a saved session references. |
| C3 | High | OK (fixed + e2e) | `viewer.ts unloadModel` | **unloadModel never persisted -> model resurrection** — a reload before any other persist re-added the explicitly-removed model. -> persistLocalState() at end of unloadModel (composes with C2 eviction). e2e: unload -> reload -> model stays gone. |
| C4 | High | OK (fixed + e2e) | `viewer.ts applyRestoredViewState` (~3819) | **Section planes duplicated on manual Restore** — re-added persisted planes without clearing first, stacking duplicates + leaking gizmo helpers + re-persisting the doubled list. -> `clearSections(false)` at the start of the section branch (mirrors the viewpoint-restore path). e2e: Restore twice -> still exactly one plane. |
| C5 | Low | OK (fixed + unit) | `viewer.ts restoreSectionSlider` + `tools/section.ts sectionPlanePercent` | **Section slider inert after restore** — restore recreated the plane geometrically but never set activeSectionNormal/Box/Label, slider position, or unhid the slider, so setSectionPosition early-returned. -> Restore a single-axis section's slider state; slider % derived from the restored origin via new pure `sectionPlanePercent` (inverse of sectionPlanePoint), unit-tested (round-trip/clamp/degenerate). No schema bump needed. |
| C6 | High | OK (fixed) | `viewer.ts persistLocalState`/`saveSession` (~3645) | **saveSession reported success even when persistence failed** — persistLocalState swallowed quota errors (red toast) but returned normally, so saveSession always fired the green toast (contradictory toasts + false success). -> persistLocalState returns a boolean; saveSession only shows the success toast when the write actually succeeded (quota failure already shows its own red toast). |
| E1 | High | OK (fixed + unit) | `tools/edges.ts EdgeGeometryCache.prune` + `viewer.ts unloadModel` | **EdgeGeometryCache never evicted on unload -> GPU geometry leak** — the per-source-uuid cache was freed only in dispose() (teardown); repeated load/unload with edges enabled grew GPU memory unbounded (tablet/field persona). -> Add `prune(keepUuids)` (dispose+drop entries whose uuid isn't live); unloadModel calls it after disposeModel with the live source-geometry uuids. No visual change. |
| P1 | High | OK (fixed + unit + build-verified) | `scripts/pwa-plugin.mjs` + `src/sw-template.js` | **embed.html not precached -> offline embed served the full-app shell** — the plugin hard-coded only index.html. -> Enumerate the configured rollupOptions.input HTML entries (`htmlEntriesFromInput`) into the precache (index.html AND embed.html); SW navigate fallback is now PATH-AWARE (`matchShell`): a /embed navigation falls back to embed.html, else index.html. Verified against a real build (dist/sw.js lists both). |
| P2 | Med | OK (fixed) | `src/sw-template.js` navigate handler | **SW cached non-OK navigations -> poisoned offline shell** — an unconditional cache.put let a transient 500/503 overwrite the good cached `/`. -> Only cache a navigation when `response.ok && response.type === 'basic'`; on a non-ok/thrown response prefer the precached (path-aware) shell over a cached error. |
| M1 | Low | OK (fixed + unit) | `core/model-index.ts` (~83) | **Parallel index unbounded Promise.all** — materialized every chunk's ItemData at once (peak-memory spike on large models). -> Bound to INDEX_CHUNK_CONCURRENCY(=12)-sized batches, folded in chunk order before the next batch — bounded peak memory, deterministic maps preserved. Unit test asserts peak in-flight <= 12 + deterministic build. |
| M2 | Quality | OK (fixed) | `vite.config.ts` manualChunks comment + `embed.ts:149` comment | **Inaccurate comments** — (a) referenced a non-existent `core/ifc-conversion.ts` (real: `core/ifc-conversion-client.ts` + `workers/ifc-conversion.worker.ts`); (b) claimed the shell excludes three, but three is statically imported so it DOES ship (only @thatopen/web-ifc are deferred). -> Both comments corrected to state reality. |
| M3 | Low | OK (fixed) | `tests/unit/frag-cache-idb.spec.ts` | **IndexedDbFragCache error-path coverage** — added unit tests with a hand-rolled fake indexedDB/IDBRequest/IDBTransaction (no external dep, C1): open-failure -> caught rejection; readwrite put rejects on tx.onabort (guards C1); get returns null on missing key. |

#### Review-hardening (3 non-blocking residuals from the post-fix adversarial re-verification — all fixed)

| ID | Sev | Verdict | Location | Issue -> Fix |
|----|-----|---------|----------|-------------|
| C2-race | Med | OK (fixed + unit) | `viewer.ts evictUnreferencedFrag` + `core/frag-cache.ts shouldEvictFragKey` | **Frag-eviction unload/load race** — `referenced` was snapshotted at the start of the async prune; unload A then immediately load a DIFFERENT model B whose `put` lands mid-prune could delete B's fresh frag (B works this session but a future restore silently re-converts). -> New pure `shouldEvictFragKey(key, referenced, storedAt, pruneStartedAt)`: RE-COMPUTE the referenced set (live + persisted) immediately before each delete, AND never delete an entry whose `storedAt` is newer than the prune's start. Unit test proves B survives while an older orphan is still evicted. |
| E1-order | Low | OK (fixed) | `viewer.ts unloadModel` | **prune-before-applyEdges ordering** — `edgeGeometryCache.prune` ran before `applyEdges()` cleared the stale overlays, so for a synchronous window the live `edgeOverlays` referenced disposed geometry (safe today, fragile). -> Reordered: `applyEdges()` (tears down + rebuilds overlays from live modelObjects) runs first, then `prune()` disposes the orphaned geometry. Same net result. |
| P1/P2-test | Med | OK (fixed + unit + e2e) | `src/sw-logic.js` + `tests/unit/sw-logic.spec.ts` + `e2e/pwa.spec.ts` | **P1/P2 had no behavioral regression test.** -> Extracted the pure decisions (`pickShellName`, `shouldCacheNavigation`) into `src/sw-logic.js`, inlined into `sw.js` at build time (one source of truth). Unit tests both decisions; e2e: offline `/embed` serves the chromeless embed shell (not the full app), and a 503 navigation does not poison the cached shell. |

### Deferred to W6 (recorded, NOT implemented in W5-fixups)

- **web-ifc lazy IfcLoader setup in `bootstrapEngine`** — web-ifc is self-hosted (no C1 violation) and the initial-shell exclusion goal is already met via the conversion worker chunk, so only the M2 comment was corrected; the lazy-IfcLoader refactor is deferred.
- **SW `skipWaiting`/`clients.claim` deferral** — the current network-fallback + immutable-asset strategy mitigates the risk; the narrow residual is an offline worker-chunk fetch immediately after a deploy (before the new SW's precache completes). Known limitation, deferred.
